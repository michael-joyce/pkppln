{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Whitelist creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('whitelist') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
