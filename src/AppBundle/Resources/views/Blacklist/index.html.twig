{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Blacklist list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Uuid</th>
                <th>Comment</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('blacklist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.uuid }}</td>
                <td>{{ entity.comment }}</td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('blacklist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('blacklist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('blacklist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
