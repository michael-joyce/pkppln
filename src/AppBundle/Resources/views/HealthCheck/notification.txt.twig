This is an automated notification. 

The following OJS Journals have not contacted the PKP PLN Staging Server in 
than {{ days }} days. 

{% for journal in journals %}
    {{ journal.title }} - {{ journal.url }}
    Most recent contact: {{ journal.contacted|date }}
    {% if journal.publisherName %}Published by {{ journal.publisherName }}{% endif %}
    
    {% if journal.publisherUrl %}{{ journal.publisherUrl }}{% endif %}
    
    This journal has made {{ journal.deposits|length }} deposits.
{% endfor %}

Please check with the journal and/or the publishers to see if the journal
should be triggered.

Journal status can be updated at {{ base_url }}.