{% extends '::base.html.twig' %}


{% block pageheader %}
    <h1>Deposit List</h1>
{% endblock %}

{% block body -%}

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

    <table class="records_list table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Deposit Uuid</th>
                <th>Received</th>
                <th>Action</th>
                <th>Journal</th>
                <th>Volume</th>
                <th>Issue</th>
                <th>Pubdate</th>
                <th>Url</th>
                <th>Size</th>
                <th>State</th>
                <th>Plnstate</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                    <td><a href="{{ path('deposit_show', { 'id': entity.id }) }}">{{ entity.deposituuid }}</a></td>
                <td>{% if entity.received %}{{ entity.received|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.action }}</td>
                    <td><a href="{{ path('journal_show', { 'id': entity.journal.id }) }}"> {{ entity.journal.title }} </a></td>
                <td>{{ entity.volume }}</td>
                <td>{{ entity.issue }}</td>
                    <td>{% if entity.pubDate %}{{ entity.pubDate|date('Y-m-d') }}{% endif %}</td>
                    <td><a href="{{ entity.url }}">{{ entity.url }}</a></td>
                <td>{{ entity.size }}</td>
                <td>{{ entity.state }}</td>
                <td>{{ entity.plnState }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(entities) }}
    </div>

{% endblock %}
